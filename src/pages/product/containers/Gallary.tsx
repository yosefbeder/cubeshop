import React, { useState } from 'react';
import classes from '../product.module.css';

interface GallaryProps {
  imgSrcs: string[];
}

const Gallary: React.FC<GallaryProps> = ({ imgSrcs }) => {
  return <></>;
};

export default Gallary;
